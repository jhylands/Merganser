package com.mygdx.game;

import com.badlogic.gdx.graphics.Texture;

public class MapLoader {
	
		/*public void CreatOne(xml one){
			//create new map class
			Map aMap = new Map();
			//get the texture of that class from the XML element
			aMap.setBackground(new Texture(one.background.text));
			//get the portals from the XML element
			//I'm having to copy and paste portals,walls,blocks -> can we make them more generic?
			aMap.features = new MapFeature[one.features.length];
			
			aMap.portals = new Portal[one.portals.length];
			for(int i=0;i<one.portals.length){
				aMap.portals[i] = new PortalFromXML(one.portals[i]);
			}

			
		}*/
}
